{
  "name": "precisely-web",
  "description": "",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "postinstall": "cd app-backend && npm install && cd ../app-client && npm install",
    "server": "cd app-backend && npm start",
    "client": "cd app-client && npm start",
    "all": "npm run server | npm run client"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/precisely/web"
  },
  "dependencies": {},
  "devDependencies": {}
}
