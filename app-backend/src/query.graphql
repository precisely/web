#import "./user-data-map/api/schema.graphql"
#import "./genetics-service/api/schema.graphql"

type GenericResponse {
    success: Boolean,
    message: String
}

type Query {
    listUserDataMap(limit: Int, offset: Int): [UserDataMap],
    geneticsList(limit: Int, lastEvaluatedKey: String, createdAt: String): GeneticsList,
    getGeneticsData(data_type_user_id: String!): Genetics
}

type Mutation {
    findOrCreateUserDataMap(userId: String!, vendorDataType: String!): UserDataMap,
    updateGenetics(gene: String, source: String, variant: String, quality: String, data_type_user_id: String!): Genetics,
    createGenetics(
            gene: String!,
            source: String!,
            variant: String!,
            quality: String,
            data_type_user_id: String!
    ): Genetics,
}
