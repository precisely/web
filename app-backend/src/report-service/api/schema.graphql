type Report {
  id: String,
  title: String,
  slug: String,
  rawContent: String,
  parsedContent: String,
  topLevel: Boolean,
  genes: [String],
  createdAt: String
  updatedAt: String
  userData(vendorDataType: String!): UserData,
}

type UserData {
  genotypes: [Genotype]
}