{
  "name": "precisely-web-aws-init",
  "description": "",
  "private": true,
  "scripts": {
    "deploy": "yarn sls deploy",
    "env": "yarn env:deploy",
    "env:deploy": "ENV=${ENV:-deploy} ../env/withenv",
    "sls": "yarn env sls"
  },
  "author": "Precise.ly, Inc",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^1.28.0"
  }
}
